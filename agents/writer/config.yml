# 写作智能体描述文件
name: "WriterAgent"
version: "1.0.0"
description: "核心执行节点，负责读取上下文并根据短期情节扩写正文草稿。"

# 路由订阅
subscribe_command: "DRAFT_CHAPTER"

# 模型调用偏好 (可留作扩展)
llm_preferences:
  temperature: 0.8
  max_tokens: 8192