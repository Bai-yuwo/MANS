{# prompts/user/experts/RuleSmith/design_rules.j2
   渲染变量(对齐 RuleSmith.input_schema):
     world_brief: str (required)
     genre: str (required)
     power_genre: str
     cultivation_hint: str
     themes: list[str]
#}

# 任务:设计世界规则与体系节点

世界观纲要:
{{ world_brief }}

题材:{{ genre }}
{% if power_genre %}
力量类型:{{ power_genre }}
{% endif %}
{% if themes %}
主题:{{ themes | join("、") }}
{% endif %}

## 根据题材产出对应类型的节点草案

请严格根据题材决定产出哪些节点，不要为不匹配的题材生成 cultivation/tech/social 节点：

- **若题材为修真/玄幻/仙侠/武侠/内力** → 产出 `cultivation_nodes` + `cultivation_chain`
  - 节点示例: name="练气期", tier=1, abilities=["吸纳天地灵气"], limitations=["每日仅能运转三个周天"]
- **若题材为科幻/未来/赛博/机甲/高科技** → 产出 `tech_nodes` + `tech_tree`
  - 节点示例: name="核聚变能源", tier=1, effects=["提供近乎无限的清洁能源"], prerequisites=["超导材料技术"]
- **若题材为历史/宫斗/权谋/都市/职场/王朝** → 产出 `social_nodes` + `social_system`
  - 节点示例: name="九品中正制", node_type="institution", privileges=["品级决定仕途上限"], obligations=["定期向朝廷举荐人才"]
- **若题材为西幻/魔法/巫师** → 产出 `cultivation_nodes`(映射为魔法等级) + `setting_nodes`(category=magic)
  - 魔法等级节点示例: name="学徒法师", tier=1, abilities=["施展基础照明术"]
- **任何题材** → 必须产出 `world_rules` 8-15 条

**关键约束**：
1. 不匹配的节点字段请留空数组 `[]` 或空对象 `{}`，不要强行套用其他体系。
2. 若 genre 是混合题材(如"玄幻+科技")，可组合产出 cultivation_nodes + tech_nodes。
3. 若题材不明确，在 reasoning 中说明判断依据，不要猜测。
